setopt INTERACTIVE_COMMENTS
setopt APPEND_HISTORY
setopt INC_APPEND_HISTORY
setopt HIST_IGNORE_DUPS
setopt HIST_IGNORE_SPACE

HISTFILE="$HOME/.zsh_history"
HISTSIZE=1000000
SAVEHIST=1000000

eval "$(/opt/homebrew/bin/brew shellenv)"
fpath=($HOME/.docker/completions $fpath)
autoload -Uz compinit && compinit
autoload -U bashcompinit && bashcompinit

# fzf
source /opt/homebrew/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
source /opt/homebrew/share/zsh-autosuggestions/zsh-autosuggestions.zsh
source <(fzf --zsh)

# starship
eval "$(starship init zsh)"

# direnv
eval "$(direnv hook zsh)"

# kubectl
source <(kubectl completion zsh)

# terraform
complete -o nospace -C /opt/homebrew/bin/terraform terraform

# gcloud-sdk
#export PATH="$HOME/google-cloud-sdk/bin:$PATH"
#if [ -f "$HOME/google-cloud-sdk/path.zsh.inc" ]; then . "$HOME/google-cloud-sdk/path.zsh.inc"; fi
#if [ -f "$HOME/google-cloud-sdk/completion.zsh.inc" ]; then . "$HOME/google-cloud-sdk/completion.zsh.inc"; fi

#export AWS_CA_BUNDLE=$HOME/Library/iag_ca_cert_chain.pem
#export CURL_CA_BUNDLE=$HOME/Library/iag_ca_cert_chain.pem
#export GIT_SSL_CAINFO=$HOME/Library/iag_ca_cert_chain.pem
#export GRPC_DEFAULT_SSL_ROOTS_FILE_PATH=$HOME/Library/iag_ca_cert_chain.pem
#export NODE_EXTRA_CA_CERTS=$HOME/Library/iag_ca_cert_chain.pem
#export REQUESTS_CA_BUNDLE=$HOME/Library/iag_ca_cert_chain.pem

export PATH="$HOME/.local/bin:$PATH"

# OpenClaw Completion
[ -f "$HOME/.openclaw/completions/openclaw.zsh" ] && source "$HOME/.openclaw/completions/openclaw.zsh"
